{
  "name": "@switchboard/ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Completion and embedding providers: Groq (per-message), Gemini (assistant), local (embeddings).",
  "exports": {
    ".": "./src/index.ts",
    "./groq": "./src/groq.ts",
    "./summarize": "./src/summarize.ts",
    "./provider": "./src/provider.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0"
  }
}
