{
  "name": "@switchboard/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Queue consumer: normalize, embed, extract. Runs warm on Azure Container Apps.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@switchboard/core": "workspace:*",
    "@switchboard/db": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsup": "^8.5.1",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  }
}
